const cards = document.querySelectorAll('.toggleFAQcard');cards.forEach(card => { card.addEventListener('click', () => { if (card.classList.contains('active')) return; cards.forEach(c => c.classList.remove('active')); card.classList.add('active'); });});document.addEventListener('click', e => { if (e.target.matches('button') && e.target.previousElementSibling?.matches('p')) navigator.clipboard.writeText(e.target.previousElementSibling.textContent) .then(() => console.log('copied text'), error => console.error('failed to copy', error));}); let lastScrollTop = 0;const movingBox = document.getElementById('movingFAQbox');const triggerHeight = window.innerHeight * 0.90;window.addEventListener('scroll', () => { let currentScroll = window.pageYOffset || document.documentElement.scrollTop; if (currentScroll < lastScrollTop) { movingBox.style.paddingTop = '80px'; } else { movingBox.style.paddingTop = '0px'; } lastScrollTop = currentScroll <= 0 ? 0 : currentScroll;}); const faqNavBar = document.getElementById('faqNavBar');const triggerPoint = 1000 - faqNavBar.offsetHeight;window.addEventListener('scroll', () => { if (window.scrollY >= triggerPoint) { faqNavBar.classList.remove('bottom-sticky'); faqNavBar.classList.add('top-sticky'); } else { faqNavBar.classList.remove('top-sticky'); faqNavBar.classList.add('bottom-sticky'); }});function highlightActiveBookmark() { const currentHash = window.location.hash; const navLinks = document.querySelectorAll('.navFAQlink'); navLinks.forEach(link => { if (link.getAttribute('href') === currentHash) { link.style.color = ''; link.style.fontWeight = ''; } else { link.style.color = ''; link.style.fontWeight = ''; } });} document.addEventListener("DOMContentLoaded", () => { const sections = document.querySelectorAll("section"); const navLinks = document.querySelectorAll(".navFAQlink"); const observerOptions = { root: null, rootMargin: "-10% 0px -60% 0px", threshold: 0, }; const observerCallback = (entries) => { entries.forEach((entry) => { if (entry.isIntersecting) { const id = entry.target.getAttribute("id"); navLinks.forEach((link) => link.classList.remove("active")); navLinks.forEach((link) => link.classList.add("inactive")); const activeLink = document.querySelector(`.navFAQlink[href="#${id}"]`); if (activeLink) { activeLink.classList.add("active"); } } }); }; const observer = new IntersectionObserver(observerCallback, observerOptions); sections.forEach((section) => observer.observe(section));}); const faqBannerImage = document.getElementById('.faqBannerImage');const trigger = 200 - faqBannerImage.offsetHeight;window.addEventListener('scroll', () => { if (window.scrollY >= trigger) { faqNavBar.classList.remove('containerStart'); faqNavBar.classList.add('containerEnd'); } else { faqNavBar.classList.remove('containerEnd'); faqNavBar.classList.add('containerStart'); }});const modal = document.getElementById('iframeModal');const iframe = document.getElementById('modalIframe');const openBtn = document.getElementById('openModalBtn');const closeBtn = document.getElementById('closeModalBtn');const targetUrl = 'https://example.com'; openBtn.addEventListener('click', () => { iframe.src = targetUrl; modal.showModal(); });closeBtn.addEventListener('click', () => { modal.close(); iframe.src = ''; });modal.addEventListener('click', (e) => { if (e.target === modal) { modal.close(); iframe.src = ''; }});